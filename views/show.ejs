<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/bootstrap.css" rel="stylesheet" type="text/css">
    <link href="/css/style.css" rel="stylesheet" type="text/css">
    <script defer src="/js/bootstrap.bundle.js"></script>
    <title>Profile</title>
</head>
<body class="body">
    <header class="header text-center">
        <h1 class="display-1">Welcome, <%= user.name %> </h1>
        <nav class="account-controls">
            <a href="/index/<%= user.account %>/edit" class="btn btn-primary">Settings</a>
            <a href="/index" class="btn btn-primary">Logout</a>
        </nav>
    </header>
   
    <h3 class="text-center"><%= user.name  %>'s Profile. </h3>
    
    <main class="character-container d-flex  justify-content-center flex-wrap">

       <%   if (user.avatars.length < 1){ %>
            <h4>No Avatars Available.</h4>
        <%   }else { %>
        <% for(i = 0; i < user.avatars.length ; i++){ %> 
        <%- include('partials/avatar-show.ejs') %>
        <% } %>  
        <% } %> 
    </main>
    <a href="/index/<%= user.account %>/new" class="btn btn-success">New Character</a>

</body>
</html>
<% console.log(user.account) %>
<% console.log(user) %>